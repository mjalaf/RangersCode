version: '3.9'

services:
  default-api:
    image: ${REGISTRY:-ranger}/default-api:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: Services/poc/ranger-default-api/Dockerfile