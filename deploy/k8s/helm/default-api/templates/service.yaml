apiVersion: v1
kind: Service
metadata:
  name: {{ include "default-api.fullname" . }}
  labels:
    {{- include "default-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "default-api.selectorLabels" . | nindent 4 }}
